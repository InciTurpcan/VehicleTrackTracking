@model CreatePartDto
@{
    ViewData["Title"] = "CreatePart";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-content">
    <div class="container-fluid">

        <!-- page content -->
        <div class="row">
            <div class="col-xl-12">

                <div class="card">
                    <div class="card-body">

                        <h4 class="card-title">Add New Part</h4>

                        <form method="post">
                            <div class="form-group">
                                <label for="exampleInputPlatePartName">Part Name</label>
                                <input type="text" class="form-control" asp-for="Name" id="exampleInputName" placeholder="Add Part Name">
                            </div>
                            <div class="form-group my-3">
                                @Html.DropDownListFor(x => x.PartCategoryId, (List<SelectListItem>)ViewBag.partCategories, new { @class = "form-control" })
                            </div>
                           

                            <button type="submit" class="btn btn-primary waves-effect waves-light">Save</button>
                        </form>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

